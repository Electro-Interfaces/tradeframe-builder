/**
 * Демонстрационное наполнение базы знаний компании (часть B раздела «Инфо») ПРИМЕРАМИ:
 * законодательство РФ по АЗС, нормы по нефтепродуктам, учёт ГСМ, промбезопасность, ЛНД, контакты.
 *
 * Все материалы помечены как ДЕМОНСТРАЦИОННЫЕ (тег «пример» + баннер в теле) и не являются
 * действующими документами. Скрипт идемпотентен: повторный запуск удаляет прежние примеры
 * (по тегу/маркеру) и заводит заново. НИКАКИЕ реальные данные не трогаются.
 *
 * Запуск:  DATABASE_URL=<...> KB_SEED_NETWORK=<id|external_id|code> node scripts/js/seed-kb-examples.cjs
 *          (KB_SEED_NETWORK по умолчанию '15' — ГИГ.)
 * Откат:   DATABASE_URL=<...> node scripts/js/seed-kb-examples.cjs --purge
 */
const path = require('path');
// Загрузка .env (на сервере backend лежит в app-root/, локально DATABASE_URL передаётся через env;
// dotenv НЕ переопределяет уже заданные переменные).
for (const p of [path.join(__dirname, '.env'), path.join(__dirname, '..', '.env'), path.join(__dirname, '..', '..', 'server', '.env')]) {
  try { require('dotenv').config({ path: p }); } catch { /* нет dotenv/файла — ок */ }
}
// Резолв серверных модулей: repo-layout (scripts/js → ../../server) ИЛИ серверный app-root (.) / app-root/scripts (..).
function reqServer(rel) {
  for (const base of ['../../server', '.', '..']) {
    try { return require(`${base}/${rel}`); } catch { /* пробуем следующий */ }
  }
  throw new Error(`Не найден серверный модуль: ${rel}`);
}
const pool = reqServer('db/pool');
const { markdownToPlain } = reqServer('services/kbService');

const NETWORK_REF = process.env.KB_SEED_NETWORK || '15';
const PURGE_ONLY = process.argv.includes('--purge');

const BANNER = '> ⚠️ **Демонстрационный пример.** Текст приведён для иллюстрации возможностей базы знаний компании и не является действующим нормативным документом.\n\n';
const TAG = 'пример';
const CONTACT_MARK = '(демо-пример)';

// ── Категории ──────────────────────────────────────────────────────────
const CATEGORIES = [
  { key: 'law', title: 'Законодательство РФ', icon: 'Scale', sort: 10 },
  { key: 'fuel', title: 'Нормы по нефтепродуктам', icon: 'Fuel', sort: 20 },
  { key: 'accounting', title: 'Учёт ГСМ', icon: 'Calculator', sort: 30 },
  { key: 'safety', title: 'Промышленная и пожарная безопасность', icon: 'ShieldAlert', sort: 40 },
  { key: 'lnd', title: 'Локальные нормативные документы (ЛНД)', icon: 'FileText', sort: 50 },
  { key: 'labor', title: 'Охрана труда', icon: 'HardHat', sort: 60 },
];

// ── Статьи ─────────────────────────────────────────────────────────────
const ARTICLES = [
  {
    cat: 'law', kind: 'law', number: '116-ФЗ', effective: '1997-07-21',
    title: 'ФЗ-116 «О промышленной безопасности опасных производственных объектов»',
    tags: ['ОПО', 'промбезопасность', 'Ростехнадзор'],
    body: `## О чём документ
Федеральный закон №116-ФЗ устанавливает требования промышленной безопасности к опасным
производственным объектам (ОПО). АЗС с резервуарами хранения нефтепродуктов относятся к ОПО.

## Что важно для АЗС
- Регистрация объекта в государственном реестре ОПО.
- Лицензирование отдельных видов деятельности.
- Производственный контроль за соблюдением требований промышленной безопасности.
- Готовность к локализации и ликвидации последствий аварий.

## Ответственность
За нарушение требований промышленной безопасности предусмотрена административная и иная
ответственность. Класс опасности ОПО определяет периодичность проверок.`,
  },
  {
    cat: 'law', kind: 'law', number: 'ТР ТС 013/2011', effective: '2013-01-01',
    title: 'Технический регламент ТР ТС 013/2011 (требования к топливу)',
    tags: ['топливо', 'качество', 'регламент', 'АИ-92', 'ДТ'],
    body: `## Назначение
Технический регламент Таможенного союза 013/2011 устанавливает требования к автомобильному
и авиационному бензину, дизельному, судовому топливу и мазуту, выпускаемым в обращение.

## Что регулирует
- Экологический класс топлива (К5 и др.).
- Показатели качества: октановое/цетановое число, содержание серы, фракционный состав.
- Маркировку и сопроводительные документы (паспорт качества, декларация соответствия).

## На АЗС
Реализация топлива допускается при наличии паспорта качества партии. Хранить копии
сопроводительных документов и предъявлять по запросу контролирующих органов.`,
  },
  {
    cat: 'law', kind: 'regulation', number: 'ПП РФ №1479', effective: '2021-01-01',
    title: 'Правила противопожарного режима в РФ (для АЗС)',
    tags: ['пожарная безопасность', 'ППР', 'запреты'],
    body: `## Применение на АЗС
Правила противопожарного режима устанавливают требования пожарной безопасности при
эксплуатации объектов, включая автозаправочные станции.

## Ключевые требования
- Запрет курения и пользования открытым огнём на территории.
- Запрет заправки при работающем двигателе и в неустановленных местах.
- Содержание первичных средств пожаротушения в исправном состоянии.
- Наличие и доступность планов эвакуации и инструкций.

См. также: раздел «Промышленная и пожарная безопасность».`,
  },
  {
    cat: 'fuel', kind: 'regulation', number: 'ГОСТ 32513-2013', effective: '2015-01-01',
    title: 'ГОСТ 32513-2013. Бензины автомобильные неэтилированные',
    tags: ['ГОСТ', 'бензин', 'АИ-92', 'АИ-95', 'октановое число'],
    body: `## Предмет
Технические условия на автомобильные неэтилированные бензины (АИ-92, АИ-95, АИ-98 и др.).

## Контролируемые показатели
| Показатель | Назначение |
| --- | --- |
| Октановое число | Детонационная стойкость |
| Содержание серы | Экологический класс |
| Фракционный состав | Испаряемость, пусковые свойства |
| Давление насыщенных паров | Сезонность (летнее/зимнее) |

## На АЗС
Соответствие подтверждается паспортом качества партии. При сомнении в качестве —
отбор пробы и направление в аккредитованную лабораторию.`,
  },
  {
    cat: 'fuel', kind: 'regulation', number: 'ГОСТ 305-2013', effective: '2015-01-01',
    title: 'ГОСТ 305-2013. Топливо дизельное',
    tags: ['ГОСТ', 'дизель', 'ДТ', 'цетановое число', 'зимнее'],
    body: `## Предмет
Технические условия на дизельное топливо.

## Особенности
- Цетановое число — воспламеняемость.
- Предельная температура фильтруемости — выбор марки по сезону (летнее, зимнее, арктическое).
- Содержание серы — экологический класс.

> Зимой реализация летнего ДТ недопустима — риск парафинизации и отказов техники у клиентов.`,
  },
  {
    cat: 'fuel', kind: 'regulation', number: null, effective: null,
    title: 'Нормы естественной убыли нефтепродуктов',
    tags: ['убыль', 'хранение', 'транспортировка', 'учёт'],
    body: `## Что это
Нормы естественной убыли определяют допустимые потери нефтепродуктов при приёме, хранении
и отпуске вследствие испарения и налипания. Утверждаются профильными приказами.

## Зависимость норм
- Вид нефтепродукта (бензин испаряется сильнее дизельного топлива).
- Сезон (тёплый/холодный период).
- Тип резервуара и операции (приём, хранение, выдача).

## Применение в учёте
Фактическая недостача в пределах нормы естественной убыли не считается потерей по вине
персонала. Сверх нормы — разбирательство и оформление.

См. также: раздел «Учёт ГСМ».`,
  },
  {
    cat: 'accounting', kind: 'guide', number: null, effective: null,
    title: 'Особенности учёта ГСМ на АЗС',
    tags: ['учёт', 'ГСМ', 'остаток', 'смена', 'инвентаризация'],
    body: `## Два регистра остатка
По резервуару различают **книжный** (расчётный) и **реальный** (по уровнемеру) остаток —
их не следует смешивать. Расхождение в пределах нормы естественной убыли — норма.

## Сменный учёт
- Остатки на начало и конец смены.
- Приход (поступления) и расход (продажи) за смену.
- Сменный отчёт — первичный документ; выгрузка является его точной копией.

## Инвентаризация
Периодически сверяют реальный остаток с книжным, оформляют результаты, разбирают
расхождения сверх норм.

В приложении: разделы «Резервуары», «Сменные отчёты», «Поступления».`,
  },
  {
    cat: 'accounting', kind: 'guide', number: null, effective: null,
    title: 'Инвентаризация нефтепродуктов в резервуарах',
    tags: ['инвентаризация', 'резервуар', 'замер', 'расхождение'],
    body: `## Порядок (типовой)
1. Зафиксировать реальный остаток по уровнемеру/измерению.
2. Сопоставить с книжным остатком на момент инвентаризации.
3. Рассчитать допустимую естественную убыль.
4. Оформить расхождения: в пределах нормы — списание убыли; сверх нормы — акт и разбирательство.

## Частые причины расхождений
- Температурное расширение/сжатие топлива.
- Погрешность измерительного оборудования.
- Неучтённые операции приёма/выдачи.

> Расхождение книжного и реального остатка — повод для инвентаризации, а не признак ошибки системы.`,
  },
  {
    cat: 'safety', kind: 'regulation', number: null, effective: null,
    title: 'Требования пожарной безопасности на АЗС',
    tags: ['пожарная безопасность', 'ЧС', 'эвакуация', 'огнетушитель'],
    body: `## Запрещено на территории
- Курить и пользоваться открытым огнём.
- Заправлять транспорт при работающем двигателе.
- Заправлять в тару, не предназначенную для нефтепродуктов (кроме установленных случаев).

## Средства и готовность
- Первичные средства пожаротушения в исправном и доступном состоянии.
- Песок, кошма, схемы эвакуации на видных местах.

## Действия при возгорании
1. Прекратить отпуск топлива, обесточить ТРК (аварийный стоп).
2. Сообщить в пожарную охрану (101) и ответственному.
3. Эвакуировать людей, приступить к тушению первичными средствами при безопасности.

См. также: контакты экстренных служб в разделе «Контакты».`,
  },
  {
    cat: 'lnd', kind: 'lnd', number: 'РГ-ПТ-01', effective: '2026-01-01',
    title: 'Регламент приёмки топлива на АЗС (пример ЛНД)',
    tags: ['ЛНД', 'приёмка', 'регламент', 'слив'],
    body: `## Перед сливом
- Проверить сопроводительные документы (паспорт качества, ТТН), сверить вид и объём топлива.
- Убедиться в свободной ёмкости резервуара, отсутствии посторонних у точки слива.

## Слив
- Заземление автоцистерны, контроль герметичности соединений.
- Отбор пробы при необходимости.

## После слива
- Замер остатка, сверка принятого объёма с документами.
- Оформление поступления; расхождения сверх нормы — фиксировать актом.

В приложении: раздел «Поступления».`,
  },
  {
    cat: 'lnd', kind: 'lnd', number: 'ИО-АЗС-01', effective: '2026-01-01',
    title: 'Инструкция оператора АЗС (пример ЛНД)',
    tags: ['ЛНД', 'оператор', 'инструкция', 'смена'],
    body: `## Начало смены
- Принять смену: сверить остатки топлива и денежных средств, проверить оборудование и связь.
- Зафиксировать показания на начало смены.

## В течение смены
- Обслуживать клиентов в соответствии с правилами безопасности.
- Контролировать работу ТРК и уровни в резервуарах; сообщать об отклонениях.

## Конец смены
- Сформировать сменный отчёт, сдать смену и денежные средства.

В приложении: разделы «Сменные отчёты», «Резервуары», «Цены ТТ».`,
  },
  {
    cat: 'labor', kind: 'lnd', number: 'ИОТ-01', effective: '2026-01-01',
    title: 'Инструкция по охране труда оператора АЗС (пример ЛНД)',
    tags: ['охрана труда', 'ЛНД', 'СИЗ', 'инструктаж'],
    body: `## Общие требования
- К работе допускаются лица, прошедшие инструктаж по охране труда и пожарной безопасности.
- Применять средства индивидуальной защиты (СИЗ) согласно нормам.

## Во время работы
- Соблюдать требования при приёме топлива и обслуживании ТРК.
- Не допускать пролива нефтепродуктов; при проливе — действовать по инструкции.

## При несчастном случае
1. Прекратить работы, обеспечить безопасность.
2. Оказать первую помощь, вызвать скорую (103).
3. Сообщить ответственному и зафиксировать происшествие.`,
  },
];

// ── Контакты ────────────────────────────────────────────────────────────
const CONTACTS = [
  { name: 'Пожарная охрана (МЧС)', position: 'Экстренная служба', phone: '101', role: 'emergency', resp: 'Вызов при пожаре и возгорании' },
  { name: 'Скорая медицинская помощь', position: 'Экстренная служба', phone: '103', role: 'emergency', resp: 'Вызов при несчастном случае' },
  { name: 'Иванов Иван Иванович', position: 'Инженер по промышленной безопасности', phone: '+7 921 000-00-01', email: 'pb@example.ru', role: 'responsible', resp: 'Промышленная и пожарная безопасность' },
  { name: 'Петрова Анна Сергеевна', position: 'Специалист по охране труда', phone: '+7 921 000-00-02', email: 'ot@example.ru', role: 'responsible', resp: 'Охрана труда, инструктажи' },
  { name: 'Дежурный диспетчер сети', position: 'Круглосуточно', phone: '+7 800 000-00-00', role: 'emergency', resp: 'Аварийные ситуации, связь с сетью' },
];

async function resolveNetwork(ref) {
  const row = await pool.queryOne(
    `SELECT id::text AS id, name FROM networks WHERE id::text = $1 OR external_id = $1 OR code = $1 LIMIT 1`,
    [String(ref)],
  );
  return row;
}

async function purge(netId) {
  await pool.query(`DELETE FROM kb_articles  WHERE network_id = $1 AND $2 = ANY(tags)`, [netId, TAG]);
  await pool.query(`DELETE FROM kb_contacts  WHERE network_id = $1 AND coalesce(note,'') LIKE $2`, [netId, `%${CONTACT_MARK}%`]);
  await pool.query(`DELETE FROM kb_categories WHERE network_id = $1 AND title = ANY($2)`, [netId, CATEGORIES.map((c) => c.title)]);
}

async function main() {
  const net = await resolveNetwork(NETWORK_REF);
  if (!net) {
    console.error(`Сеть не найдена по «${NETWORK_REF}». Укажите KB_SEED_NETWORK (id/external_id/code).`);
    process.exit(1);
  }
  console.log(`Целевая сеть: ${net.name} (${net.id})`);

  await purge(net.id);
  if (PURGE_ONLY) {
    console.log('Демо-примеры удалены (--purge).');
    await pool.closePool();
    return;
  }

  // Категории
  const catId = {};
  for (const c of CATEGORIES) {
    const r = await pool.queryOne(
      `INSERT INTO kb_categories (network_id, title, icon, sort_order) VALUES ($1,$2,$3,$4) RETURNING id::text AS id`,
      [net.id, c.title, c.icon, c.sort],
    );
    catId[c.key] = r.id;
  }

  // Статьи
  let n = 0;
  for (const a of ARTICLES) {
    const bodyMd = BANNER + a.body;
    await pool.query(
      `INSERT INTO kb_articles
         (network_id, category_id, title, body_md, body_plain, status, is_current, doc_kind, doc_number,
          effective_date, tags, sort_order, published_at)
       VALUES ($1,$2,$3,$4,$5,'published',true,$6,$7,$8,$9,$10, now())`,
      [net.id, catId[a.cat] || null, a.title, bodyMd, markdownToPlain(bodyMd), a.kind, a.number,
        a.effective, [...(a.tags || []), TAG], n],
    );
    n += 1;
  }

  // Контакты
  let m = 0;
  for (const c of CONTACTS) {
    await pool.query(
      `INSERT INTO kb_contacts (network_id, full_name, position, responsibility, phone, email, note, role, sort_order)
       VALUES ($1,$2,$3,$4,$5,$6,$7,$8,$9)`,
      [net.id, c.name, c.position || null, c.resp || null, c.phone || null, c.email || null,
        `Демонстрационный контакт ${CONTACT_MARK}`, c.role || null, m],
    );
    m += 1;
  }

  console.log(`Заведено: категорий ${CATEGORIES.length}, статей ${ARTICLES.length}, контактов ${CONTACTS.length}.`);
  console.log('Все материалы помечены как демонстрационные (тег «пример»). Откат: node scripts/js/seed-kb-examples.cjs --purge');
  await pool.closePool();
}

if (require.main === module) {
  main().catch((e) => { console.error('ОШИБКА:', e.message); process.exit(2); });
} else {
  module.exports = { main, CATEGORIES, ARTICLES, CONTACTS };
}
