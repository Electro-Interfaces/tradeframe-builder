/**
 * Константы для сервиса сверки корпоративного процессинга
 */

/**
 * Допуск по времени для сопоставления транзакций (мс)
 * ±1.5 минуты (90 секунд)
 */
export const TIME_TOLERANCE_MS = 90 * 1000;

/**
 * Код системы по умолчанию в STS API (БТО = 15)
 * Используется как fallback если systemId не передан из настроек сети
 */
export const SYSTEM_ID = 15;

/**
 * Допуск по литрам для определения совпадения транзакций
 * 0.01 литра
 */
export const LITERS_TOLERANCE = 0.01;

/**
 * Допуск для определения расхождений в UI
 * 0.1 литра
 */
export const UI_DISCREPANCY_TOLERANCE = 0.1;

/**
 * Допуск по сумме для сопоставления транзакций (рубли)
 * 1 рубль
 */
export const COST_TOLERANCE = 1;

/**
 * Паттерны для извлечения номера станции из названия TradeCorp
 */
export const STATION_NAME_PATTERNS = [
  /АКАЗС[- ]?(\d+)/i,
  /АЗС[- ]?(\d+)/i,
  /[Сс]танция[- ]?(\d+)/i,
  /[#№](\d+)/
];

/**
 * Порядок сортировки статусов транзакций
 * Расхождения сначала, совпадения в конце
 */
export const TRANSACTION_STATUS_ORDER = {
  only_corp: 0,
  only_tf: 1,
  mismatch: 2,
  matched: 3
} as const;
