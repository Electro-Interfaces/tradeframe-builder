# TradeControl - Environment Configuration
# Копируйте этот файл в .env и заполните своими данными

# ===========================================
# ОСНОВНЫЕ НАСТРОЙКИ ПРИЛОЖЕНИЯ
# ===========================================

# База URL для API (обычно совпадает с VITE_BASE_URL)
VITE_API_URL=http://localhost:3000

# Базовый URL приложения
VITE_BASE_URL=http://localhost:3000

# ===========================================
# НАСТРОЙКИ SUPABASE
# ===========================================

# URL вашего проекта Supabase (найти в Settings > API)
VITE_SUPABASE_URL=https://your-project.supabase.co

# Анонимный ключ (anon, public) - безопасно для frontend
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Service Role ключ - ТОЛЬКО для разработки, НЕ для production!
# Используется для прямого доступа к БД в development режиме
VITE_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ===========================================
# BACKEND CONFIGURATION (Server-side only)
# ===========================================

# Supabase service role key (ТОЛЬКО для сервера - не добавляйте VITE_ префикс!)
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# JWT Secret для подписи токенов
JWT_SECRET=tradeframe-jwt-secret-change-in-production

# JWT Refresh Secret
JWT_REFRESH_SECRET=tradeframe-refresh-secret-change-in-production

# Database URL для прямого подключения
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================

# Режим отладки (включается автоматически в dev режиме)
# VITE_DEBUG_MODE=true

# Порт для API сервера
API_PORT=3001

# ===========================================
# PRODUCTION SETTINGS
# ===========================================

# Для продакшн окружения:
# VITE_API_URL=https://api.tradeframe.production.com/api/v1
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-production-anon-key
# SUPABASE_SERVICE_KEY=your-production-service-key
# JWT_SECRET=your-secure-production-jwt-secret
# JWT_REFRESH_SECRET=your-secure-production-refresh-secret

# ===========================================
# NOTES
# ===========================================

# 1. Переменные с префиксом VITE_ доступны в браузере
# 2. Никогда не добавляйте секретные ключи с префиксом VITE_
# 3. Используйте .env.local для локальных переопределений
# 4. .env файлы не должны попадать в git (добавьте в .gitignore)

# Приоритет файлов конфигурации:
# .env.local > .env > .env.example