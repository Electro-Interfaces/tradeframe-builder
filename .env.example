# TradeFrame Builder - Environment Variables Example
# Скопируйте этот файл в .env для локальной разработки

# ===========================================
# API CONFIGURATION
# ===========================================

# URL для подключения к реальному API
# По умолчанию: http://localhost:3000/api/v1
VITE_API_URL=http://localhost:3000/api/v1

# Принудительное использование HTTP API вместо mock данных
# По умолчанию: false (используются mock данные в localStorage)
VITE_USE_HTTP_API=false

# ===========================================
# DEVELOPMENT SETTINGS
# ===========================================

# Режим отладки (включается автоматически в dev режиме)
# VITE_DEBUG_MODE=true

# ===========================================
# PRODUCTION SETTINGS
# ===========================================

# Для продакшн окружения:
# VITE_API_URL=https://api.tradeframe.production.com/v1
# VITE_USE_HTTP_API=true

# ===========================================
# AUTHENTICATION (будущее расширение)
# ===========================================

# API ключи и токены (если потребуется)
# VITE_API_KEY=your-api-key-here
# VITE_AUTH_TOKEN=your-auth-token-here

# ===========================================
# MONITORING & ANALYTICS (будущее расширение) 
# ===========================================

# Настройки мониторинга и аналитики
# VITE_SENTRY_DSN=your-sentry-dsn-here
# VITE_ANALYTICS_ID=your-analytics-id-here

# ===========================================
# NOTES
# ===========================================

# 1. Переменные с префиксом VITE_ доступны в браузере
# 2. Никогда не добавляйте секретные ключи с префиксом VITE_
# 3. Используйте .env.local для локальных переопределений
# 4. .env файлы не должны попадать в git (добавьте в .gitignore)

# Приоритет файлов конфигурации:
# .env.local > .env > .env.example